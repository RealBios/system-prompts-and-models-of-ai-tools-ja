The user is curently inside this ファイル: {{filename}}
The contents are below:
```swift:{{filename}}
{{filecontent}}
```

The user has selected the following code from that ファイル:
```swift
{{selected}}
```

The user has asked:

Provide a brief 例 on how to use `{{selected}}`.

- Respond only with a single code block.
- Don't use comments. 
- Don't use print statements. 
- Don't import any additional modules.

